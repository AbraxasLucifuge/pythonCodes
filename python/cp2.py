#Using a built-in module find the square root of pi and then raise the
#result to power of. Do try to use an alternative name for the built-in
#module.
import math as m
#print(m.pi)
print(m.pow(m.sqrt(m.pi),3))